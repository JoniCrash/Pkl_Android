<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Layout"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ContohDashboard"
            android:exported="false"
            android:label="@string/title_activity_contoh_dashboard"
            android:theme="@style/Theme.Layout" />
        <activity
            android:name=".ContohMasuk"
            android:exported="false"
            android:label="@string/title_activity_contoh_masuk"
            android:theme="@style/Theme.Layout" />
        <activity
            android:name=".ContohDaftar"
            android:exported="false"
            android:label="@string/title_activity_contoh_daftar"
            android:theme="@style/Theme.Layout" />
        <activity
            android:name=".Dashboard"
            android:exported="false"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/Theme.Layout" />
        <activity
            android:name=".Masuk"
            android:exported="false"
            android:label="@string/title_activity_masuk"
            android:theme="@style/Theme.Layout" />
        <activity
            android:name=".Daftar"
            android:exported="false"
            android:label="@string/title_activity_daftar"
            android:theme="@style/Theme.Layout" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Layout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>